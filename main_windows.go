package main

import "fmt"

func getAnyConnectPath() (string, error) {
	return "", fmt.Errorf("unfortunately I don't know where the anyconnect path is on windows, but a pull-request would be welcome!")
}
